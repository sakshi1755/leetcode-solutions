class Solution {
public:
    bool uniformArray(vector<int>& nums1) {
        // bool isodd=false;
        // int n=nums1.size();
        // bool iseven=false;
        // for(int i=0;i<n;i++){
        //     if(nums1[i]%2==0)iseven=true;
        //     else isodd=true;

        // }
        // if(isodd^iseven)return true;
        // int mini=*min_element(nums1.begin(),nums1.end());
        // if(mini%2==0)return false;
        // else return true;
        return true;
    }
};